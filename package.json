{
  "name": "pregoneros-de-medellin",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "r.js -o app-build.js && cp -R content public && cp -R fonts public && cp -R images public && cp -R seo public && cp -R style public"
  },
  "dependencies": {
    "ejs": "^2.7.4",
    "requirejs": "^2.3.6"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tdurand/pregoneros-de-medellin"
  },
  "license": "MIT",
  "devDependencies": {},
  "author": ""
}
